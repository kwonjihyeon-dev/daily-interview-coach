NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
NEXT_PUBLIC_API_KEY=

# 이메일 기반 방문자 게이트(이메일-방문자-게이트_spec.md) — 서버 사이드 전용.
# 브라우저 JS는 apps/api의 존재를 모른다는 원칙에 따라 NEXT_PUBLIC_ 접두사를 붙이지 않는다.
# Route Handler(app/api/gate/verify)와 authenticatedFetch가 이 값으로 apps/api를 호출한다.
API_BASE_URL=http://localhost:3001
